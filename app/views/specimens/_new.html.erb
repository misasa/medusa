<%= form_with model: specimen, local: false, format: :json, html: { id: "new_specimen", class: "create" } do |f| %>
<tr>
  <th><%= check_box_tag nil, nil, false, class: "select-row-all" %></th>
  <th></th>
  <% @search_columns.each do |column| %>
  <th>
    <%= SpecimenDecorator.create_form(f, column, :index) %>
  </th>
  <% end %>
  <th>
    <%= f.button class: "btn btn-default" do %>
    <span class="glyphicon glyphicon-save"></span>
    <% end %>
  </th>
</tr>
<% end %>
