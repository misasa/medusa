    <% tab_info = [
        ["at-a-glance",nil,"show representative properties of a record"],
        ["daughter",@specimen.children,"show powder or thin sample made from a sample of the record"],
        ["history", @specimen.paths,"show the location and moving history of the sample"],
        ["analysis",@specimen.full_analyses,"show "],
        ["bib",@specimen.bibs,""],
        ["file",@specimen.attachment_files,""],
        ["quantity", @specimen.self_and_descendants,""]] %>
    <!-- Nav tabs -->
    <ul class="nav nav-tabs">
      <% tab_info.each do |key,array,message| %>
       <li class="<%= active_if_current(key) %>" ><a href="#<%= key %>" data-toggle="tab"><%= key + data_count(array) title="<= message>"%></a></li>
      <% end %>

    </ul>
    <!-- Tab panes -->
    <div class="tab-content">
      <% tab_info.each do |key,array| %>
        <div class="tab-pane<%= active_if_current(key) %>" id="<%= key %>"><%= render partial: key.underscore %></div>
      <% end %>
    </div>
