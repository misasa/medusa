<tr>
  <th>age</th>
  <th><%= table.age_unit %></th>
  <% table.specimens.each do |specimen| %>
    <td><%= raw(specimen.age_in_text(:unit => table.age_unit, :scale => 0) || "-") %></td>
  <% end %>
  <% if table.with_average %>
    <td></td>
  <% end %>  
</tr>
