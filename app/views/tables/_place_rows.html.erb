<tr>
  <th>latitude</th>
  <th></th>
  <% table.specimens.each do |specimen| %>
    <td><%= specimen.place.try!(:latitude) || "-" %></td>
  <% end %>
  <% if table.with_average %>
    <td></td>
  <% end %>
</tr>
<tr>
  <th>longitude</th>
  <th></th>
  <% table.specimens.each do |specimen| %>
    <td><%= specimen.place.try!(:longitude) || "-" %></td>
  <% end %>
  <% if table.with_average %>
    <td></td>
  <% end %>
</tr>
