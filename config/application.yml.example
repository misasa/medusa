defaults: &defaults
  backup:
    files:
      dir_path: "/tmp/medusa/backup/files"
    db:
      dir_path: "/tmp/medusa/backup/db"

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults

staging:
  <<: *defaults

timemachine:
  <<: *defaults
